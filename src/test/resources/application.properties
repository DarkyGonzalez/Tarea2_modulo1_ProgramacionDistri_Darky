spring.application.name=libreria.spring

# Configuración de conexión para pruebas (usa la misma BD)
spring.datasource.url=jdbc:mysql://localhost:3306/dblibreria1?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Darky*2025
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Dialecto correcto para Hibernate 7.x / Spring Boot 4.x
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Esquema y logging
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

